{
	"name": "signup-processor",
	"version": "1.0.0",
	"type": "module",
	"dependencies": {
		"@text-me-when/shared": "file:../../shared",
		"pg": "^8.13.3"
	},
	"scripts": {
		"build": "esbuild index.ts --bundle --platform=node --target=es2020 --outfile=dist/index.js --format=esm --external:@text-me-when/shared --sourcemap",
		"clean": "rm -rf dist",
		"type-check": "tsc --noEmit"
	},
	"devDependencies": {
		"@types/aws-lambda": "^8.10.147",
		"@types/node": "^22.10.1",
		"esbuild": "^0.25.0",
		"typescript": "^5.2.2"
	}
}
