{
	"name": "signup-processor",
	"version": "1.0.0",
	"type": "module",
	"dependencies": {
		"pg": "^8.13.3"
	},
	"scripts": {
		"build": "node build.js",
		"build:lambda": "node build.js --mode=lambda",
		"clean": "rm -rf dist",
		"type-check": "npx tsc --noEmit"
	},
	"devDependencies": {
		"@types/aws-lambda": "^8.10.147",
		"@types/node": "^22.10.1",
		"@types/pg": "^8.10.9",
		"esbuild": "^0.25.0",
		"typescript": "^5.2.2"
	}
}
